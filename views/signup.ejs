<!DOCTYPE html>
<html lang="en">

<head>
    
  <%- include('inc/metatag.ejs') %>
    
    <title>Sign up</title>
    <link rel="stylesheet" href="/bootstrap/bootstrap.min.css">
    <link rel="stylesheet" href="/css/random.css">
    <script src="/js/jquery.min.js"></script>
    <script src="/js/html2canvas.min.js"></script>
</head>

<body>
    <div class="container ">
        <form method="POST" class="login" action="/signup">
            <% try{ if (message !==undefined) { if (status !==undefined) { %>
                <div class="alert alert-<%= status %>">
                    <%= message %>
                </div>
                <% } } } catch(err){ console.log('adduser: message or something is not defined') } %>

                    <h1>Sign up</h1>
                    <div class="form-group">
                        <input value="123@231.123" value="<%= (locals.email) ? email : '' %>" type="email"
                            class="form-control" placeholder="email" name="email">
                    </div>
                    <div class="form-group">
                        <input value="12321" value="<%= (locals.fullname) ? fullname : '' %>" type="text"
                            class="form-control" placeholder="fullname" name="fullname">
                    </div>
                    <div class="form-group">
                        <input value="<%= (locals.username) ? username : '' %>" type="text" class="form-control"
                            placeholder="username" name="username" autocomplete="username">
                    </div>
                    <div class="form-group">
                        <input value="<%= (locals.userpass) ? userpass : '' %>" type="password" class="form-control"
                            placeholder="userpass" name="userpass" autocomplete="current-password">
                    </div>
                    <div class="form-group">
                        You are already member? <a href="/login">Come here</a>
                    </div>
                    <button type="submit" class="btn btn-primary">Sign up</button>
        </form>
    </div>
    <div class="random" id="profile"></div>
    
  <!-- <script src="/js/random.js"></script>
    <script>
        html2canvas(document.querySelector("#profile")).then(canvas => {
            console.log(canvas.toDataURL('image/png'));
            document.body.appendChild(canvas)
            // sendMessage(canvas);
        });
        function sendMessage(image) {
            var msg = JSON.stringify(image);
            var xhr = new XMLHttpRequest();
            xhr.open('POST', true);
            xhr.send(msg);
            alert('file is saved');
        }
    </script> -->
</body>

</html>